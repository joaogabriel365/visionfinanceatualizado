{
  "name": "vision-finance",
  "private": true,
  "scripts": {
    "build:dashboard": "esbuild js/main.js --bundle --format=iife --platform=browser --target=es2020 --loader:.html=text --outfile=js/dashboard.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2"
  }
}
